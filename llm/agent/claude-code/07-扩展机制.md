# 07 — 扩展机制

Claude Code 的可扩展性围绕主线：**自定义 Agent**、**Skills**、**Hooks**、**MCP**，并延伸 **Workflow Scripts**、**DiscoverSkills**、插件与 Daemon/Remote Control。

---

## 自定义 Agent

### 三种来源（优先级从低到高）

| 来源 | 位置 |
|------|------|
| Built-in | 内置硬编码 agent |
| Plugin | 插件注册 |
| User/Project/Policy | .claude/agents/*.md 或 settings.json |

getActiveAgentsFromList 按 agentType 去重，**后者覆盖前者**。可在项目中放 Explore.md 完全替换内置 Explore。

### Markdown Agent 完整格式

```yaml
---
name: reviewer
description: Code review specialist
tools: Read,Glob,Grep,Bash
disallowedTools: Write,Edit
model: haiku          # 或 inherit
effort: high
maxTurns: 10
permissionMode: plan
background: true
initialPrompt: /search TODO
isolation: worktree   # 或 remote（内部）
memory: project
mcpServers: [slack]
hooks: { PreToolUse: [...] }
skills: code-review
color: blue
---
正文 = agent system prompt
```

### 加载流程

```text
loadMarkdownFilesForSubdir('agents')
  → 解析 frontmatter + 正文
  → 校验 name/description
  → 并行 loadPluginAgents
  → 合并 + 分配颜色
```

### 与 AgentTool 联动

派生时按 tools/disallowedTools 过滤；requiredMcpServers 等待连接（最长 ~30s）；runAgent 挂载 SubagentStart hooks、preload skills、agent memory、sidechain transcript。

---

## Skills 技能系统

Skills 是「Prompt + 权限配置」打包的工作流；复杂任务关键往往在 Prompt 质量。

### 触发方式（注意：`@` ≠ Skill）

| 入口 | 行为 |
|------|------|
| **用户 `/skill-name`** | 斜杠命令；插件 skill 常带命名空间前缀 |
| **模型 SkillTool** | 自主选择 skill；inline 默认把正文注入主会话 |
| **`@文件` / `@agent-…` / MCP resource** | attachments 附件路径；**不是**把 skill 正文当 `@` 主路径 |

插件只是 Skill/Agent 的**来源**（`loadedFrom: plugin` 等）；执行仍走斜杠或 SkillTool。

补充能力（当前源码）：

| 能力 | 说明 |
|------|------|
| **DiscoverSkills** | 工具面发现可用 skills（feature 门控） |
| **EXPERIMENTAL_SKILL_SEARCH / prefetch** | query 迭代内 prefetch，减少阻塞式发现 |
| **Skill + Hooks/MCP** | Agent frontmatter 可挂 hooks、mcpServers、skills |

---

### 五个来源

| 来源 | 说明 |
|------|------|
| Built-in Commands | 硬编码 70+ 斜杠命令（/commit、/review、/compact） |
| Bundled Skills | 编译时打包，延迟解压 reference files |
| 磁盘 Skills | skill-name/SKILL.md 目录格式 |
| MCP Skills | MCP server prompt 转换，禁止内联 shell |
| Legacy Commands | /commands/ 向后兼容 |

磁盘加载路径（优先级）：policy → user → project（向上遍历）→ --add-dir

### Frontmatter 关键字段

```yaml
name, description, when_to_use
allowed-tools, argument-hint, arguments
model, effort, context: inline|fork
agent, user-invocable, disable-model-invocation
version, paths（条件激活）, hooks, shell
```

### 选中后如何进入 LLM 请求

**Inline（默认）**

- 读取 `SKILL.md`：去 frontmatter，做 `${CLAUDE_SKILL_DIR}` / `${CLAUDE_SESSION_ID}` 等替换，可加 base directory 头  
- 将正文写入一条 **meta user 消息**（`isMeta: true`）——协议上是 **user 槽内容**，与用户本轮 query **同一次主采样**可见  
- 通常是 **独立一条 user**，不是与用户原话拼成同一个字符串  
- `contextModifier`：合并 allowedTools、模型/effort 覆盖；处理 `$ARGUMENTS`、`!`shell`` 等  

**Fork（`context: fork`）**

- `executeForkedSkill`：正文作为子 Agent prompt，独立跑  
- 不阻塞主线程，完成后 **task-notification**  
- 适合重型分析、大上下文隔离  

**目录 vs 正文**

- System prompt 侧有 skill **描述目录**（有 token 预算；`when_to_use` 供自动匹配）  
- **只有被触发的 skill** 才把全文灌进本轮 user 侧内容（eager inline），不是仅改 listing 等模型事后召回  

### 预算感知

Skill 描述在 system prompt 中有 token 预算，bundled skill 描述不可截断。
---

## Hooks

> 本节只描述 Claude Code 自身的 Hook 机制；不与其它脚手架对照。

Hook 是挂在 **Agent 生命周期** 上的可配置拦截点：事件触发 → 匹配 handler → 返回放行 / 改写 / 阻断 / 注入上下文等结果，再交回主循环。

### 能否自定义

**支持。** 用户、项目、策略、插件、以及 Agent/Skill frontmatter 均可声明 Hook。

| 来源 | 典型路径 | 范围 |
|------|----------|------|
| 用户全局 | `~/.claude/settings.json` 的 `hooks` | 本机所有项目 |
| 项目 | `.claude/settings.json` | 可提交进仓库 |
| 项目本地 | `.claude/settings.local.json` | 通常 gitignore |
| 托管策略 | managed / enterprise policy | 组织强制 |
| 插件 | 插件内 `hooks/hooks.json` | 启用插件时合并 |
| Agent / Skill | frontmatter `hooks:` | 该组件激活期间 |

### 事件面（节选，约 20+）

| 阶段 | 事件 |
|------|------|
| 会话 | SessionStart, SessionEnd, Setup |
| 用户 | UserPromptSubmit, Stop, StopFailure |
| 工具 | PreToolUse, PostToolUse, PostToolUseFailure |
| 权限 | PermissionRequest, PermissionDenied |
| 子 Agent | SubagentStart, SubagentStop |
| 压缩 | PreCompact, PostCompact |
| 协作 | TeammateIdle, TaskCreated, TaskCompleted |
| MCP | Elicitation, ElicitationResult |
| 环境 | ConfigChange, CwdChanged, FileChanged, WorktreeCreate/Remove |

### Handler 类型

| 类型 | 执行方式 |
|------|----------|
| command | Shell 脚本；stdin 收 JSON 事件 |
| prompt | 注入模型可见上下文 |
| agent | 拉起子 Agent |
| http | HTTP 请求 |
| callback | SDK 内部 JS |
| function | 运行时注册（Agent/Skill 内） |

### 能力边界（本脚手架内）

**常见可做：**

- PreToolUse：`deny` / `ask` / `updatedInput`（可参与权限裁决链）
- PostToolUse：审计、format、通知等副作用
- Stop：`preventContinuation` 终止；或注入阻塞错误强制再想一轮（`stop_hook_blocking`）
- 压缩前后、子 Agent 启动时改上下文
- command 变量替换：`${CLAUDE_PLUGIN_ROOT}`、`${CLAUDE_PROJECT_DIR}` 等；可标异步

**心智：** 按 **事件类型 + 约定回执** 扩展（类型化生命周期钩子），不是任意改写内部所有结构体。能力面覆盖会话、工具、权限、协作、环境，相对完整。

### 与权限链、主循环

```text
工具调用前：
  blanket 规则 → 工具 checkPermissions → PreToolUse Hook → Ask 规则 → permissionMode 默认

Stop：
  preventContinuation → 结束
  阻塞错误 → 继续 agentic loop
```

执行要点：stdin JSON、默认较长超时、`async` 可标记异步。

### 配置示例

**拦危险 Bash（PreToolUse + command）：**

```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(rm *)",
            "command": "${CLAUDE_PROJECT_DIR}/.claude/hooks/block-rm.sh"
          }
        ]
      }
    ]
  }
}
```

脚本读 stdin；以约定方式（如 exit 2 + stderr 原因）表示拦截。

**Edit/Write 后自动 format（PostToolUse）：**

```json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | xargs npx prettier --write"
          }
        ]
      }
    ]
  }
}
```

### 定位一句话

Hook = **生命周期守卫与扩展总线**（审计、策略、注入、停/续），与 Custom Agent / Skill / MCP 正交：Agent 定角色，Skill 定流程，Hook 定守卫，MCP 定外部集成。

---

## MCP 配置

### 配置来源合并

```text
settings.json mcpServers
.mcp.json（项目级）
插件 manifest
claude.ai connectors
enterprise managed-mcp.json
SDK 传入（IDE 嵌入）
内置 dynamic（computer-use, chrome, vscode）
```

getAllMcpConfigs：enterprise 独占或合并多源。

### 连接状态

connected | failed | needs-auth | pending | disabled

useManageMcpConnections React Hook 管理生命周期；connectToServer memoize 缓存。

### 认证

OAuth 等需 needs-auth 状态，用户完成认证后 reconnect。

### Agent 级 MCP

Agent definition 可声明：
- requiredMcpServers：启动前必须可用
- mcpServers：agent 专属 server，生命周期跟随子 Agent

---

## 插件系统（概要）

插件可注册：Agent、Skill、MCP server、Hooks。命名空间前缀避免冲突（如 plugin::agent-name）。

---

## 扩展机制协作示例

**代码审查 Skill + 自定义 Reviewer Agent + PreToolUse Hook**

```text
用户: /code-review src/
  → SkillTool inline 注入审查 prompt + allowedTools
  → 或 Agent({ subagent_type: "code-reviewer" })
  → PreToolUse hook 写 audit log
  → Reviewer 用 Read/Grep 分析
  → PostToolUse hook 通知 Slack（MCP）
  → 结果回主线程或 task-notification
```

---

## Workflow Scripts

`WORKFLOW_SCRIPTS` feature 下，`WorkflowTool`（`src/workflow/` + `@claude-code-best/workflow-engine`）把多 phase 编排委托给 `runAgent` 一类执行核。与「单次 Skill prompt」相比，更接近可编程流水线；transcript 可用 subdir 分组。

与 Daemon / Remote Control 同属「五层主链路外的平行子系统」，详见 [09](./09-特性与模式索引.md)。

---

## 扩展设计哲学

| 机制 | 扩展什么 |
|------|----------|
| Custom Agent | **角色与工具边界** |
| Skill / DiscoverSkills | **工作流 Prompt 与发现** |
| Hook | **生命周期拦截与审计** |
| MCP | **外部系统能力** |
| Workflow | **多 phase 编排** |
| Plugin | **上述能力的打包分发** |

四者正交：Agent 定角色，Skill 定流程，Hook 定守卫，MCP 定集成。
