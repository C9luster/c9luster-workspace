# 01 — 概述与定位

## 一句话定义

LangGraph 是一个**底层编排框架**，用来构建、管理、部署**长运行、有状态**的 Agent 与工作流。

## 技术定位：三个关键词

| 关键词 | 含义 |
|--------|------|
| **Low-level orchestration** | 提供图计算式运行时，而不是固定 Agent 模板 |
| **Stateful / durable** | 状态可持久化，失败与中断后可从检查点恢复 |
| **Multi-actor** | 多节点、可并行、可动态派发（`Send`）的多 Actor 工作流 |

官方描述（README）：

> Low-level orchestration framework for building stateful agents.

安装：

```bash
pip install -U langgraph
# 或
uv add langgraph
```

## 它解决什么问题

LangGraph 为「任意长运行、有状态的 workflow / agent」提供基础设施：

| 能力 | 说明 |
|------|------|
| Durable execution | 故障后从离开的位置继续跑 |
| Human-in-the-loop | 任意节点可中断，人工检查 / 修改状态后再继续 |
| Memory | 短时工作记忆 + 跨会话长期记忆 |
| Streaming | 节点 / 消息级流式事件 |
| Production deployment | 配合 LangSmith Deployment / CLI 部署 |

## 它不是什么

- **不是**「装完就能聊天的完整产品」——需要自己定义图或用 prebuilt
- **不是**必须绑定 LangChain 全套——可独立使用，但常依赖 `langchain-core`
- **不是** JS 版本体——JS/TS 在独立仓库 [LangGraph.js](https://github.com/langchain-ai/langgraphjs)

## 与 LangChain Agent 的分工

| | LangChain Agent | LangGraph |
|---|---|---|
| 抽象层级 | 高：预置架构 + 模型集成 | 低：自建图与状态机 |
| 适用 | 快速搭 LLM 应用 | 复杂控制流、HITL、长任务、可控延迟 |
| 关系 | 新版 agents 底层常建在 LangGraph 上 | 可不用 LangChain 业务组件，也能跑 |

经验法则：

- 只要「模型 + 工具循环」→ 优先 prebuilt / LangChain agents
- 要「确定性流程 + Agent 节点混用、人工卡点、严格恢复」→ 用 LangGraph 手写图

## 生态位置

```text
Deep Agents          ← 更高层：规划 / 子 Agent / 文件系统
        ↑
LangGraph            ← 本仓库关注的编排运行时
        ↑
langchain-core       ← Runnable、消息、工具等基础抽象
        ↔
LangSmith            ← 观测、评估、部署
```

## 设计灵感

| 来源 | 借鉴点 |
|------|--------|
| Google Pregel | 以超步（superstep）推进的图计算模型 |
| Apache Beam | 流水线 / 持久化执行思路 |
| NetworkX | 面向用户的图构建 API 风格 |

## 版本与成熟度

对照本地 monorepo 时：

- 核心包名：`langgraph`
- 版本量级：`1.x`（PyPI classifiers 为 Production/Stable）
- Python：`>=3.10`

---

*上一篇：无 · [下一篇：仓库结构与包依赖](./02-仓库结构与包依赖.md)*
