# 04 sh-mk 工具脚本

`sh-mk/` 提供 Coding Agent SFT **数据准备和效果验证** 的本地工具，配合标准训练流程使用。

## 目录结构

```
sh-mk/
├── model/              # 训练文档（本目录）
├── replay/             # 轨迹 Replay（数据采集 + 评估）
├── event/              # Agent Event 采集
├── image/              # 多模态测试
├── skill/              # Skill 管理
└── calc_coding_agent_cache_hit_rate.py
```

## 1. 数据采集：Event → 训练数据

### get_event.py

从 Agent 运行环境拉取 event 轨迹：

```bash
python sh-mk/event/get_event.py
# 产出: event/new_general_event.jsonl
```

每条 event 包含 user/assistant/tool 消息，是构建 SFT 数据的原始素材。

### build_replay_template_from_replay_json.py

将具体 replay 转为可复用模板（用于 benchmark 和数据标准化）：

```bash
python sh-mk/replay/build_replay_template_from_replay_json.py \
  --in  sh-mk/replay/replay.json \
  --out sh-mk/replay/replay.template.json \
  --chunks 5 \
  --mode message
```

功能：
- 替换具体 id 为占位符（`__app_id__`、`__conv_id__` 等）
- 移除敏感 meta 字段
- 重编号 event id

### 典型数据流水线

```
Agent 运行
  ↓ get_event.py
原始 event (jsonl)
  ↓ 筛选成功轨迹 + 格式转换
ShareGPT 格式 train.jsonl
  ↓ 注册到 LLaMA-Factory / ms-swift
SFT 训练
```

## 2. 效果验证：Replay 回放

### repaly.py

用固定 replay 模板回放 Agent 任务，对比不同模型的输出：

```bash
python sh-mk/replay/repaly.py \
  --template sh-mk/replay/replay.template.json \
  --model sft-v1
```

用于 SFT 前后的 A/B 对比。

### mk_replay_template_stream_prd.py

PRD 流式模板生成，适用于 PRD → 代码生成场景。

## 3. Token 效率分析

### calc_coding_agent_cache_hit_rate.py

从 perf_stream 日志统计 token 和 cache 命中率：

```bash
python sh-mk/calc_coding_agent_cache_hit_rate.py \
  --log /path/to/openhands.log \
  --template coding_agent
```

输出指标：
- prompt / completion / total tokens
- cache_read / cache_write
- weighted_cache_hit_rate

用于对比 SFT 前后模型的 token 消耗和成本。

## 4. 多模态测试

如果 Coding Agent 涉及图片理解（如 UI 截图 → 代码）：

```bash
# 多模态 prompt 测试
python sh-mk/image/prompt_image_test.py

# 流式 benchmark
python sh-mk/image/stream_image_benchmark_demo.py
```

## 5. 与 SFT 流程的集成点

| 阶段 | 工具 | 作用 |
|------|------|------|
| 数据采集 | `event/get_event.py` | 拉取 Agent 轨迹 |
| 数据清洗 | `replay/build_replay_template_from_replay_json.py` | 标准化轨迹格式 |
| 训练前验证 | 手动检查 train.jsonl | 确认格式正确 |
| 训练后评估 | `replay/repaly.py` | 回放 benchmark |
| 效率对比 | `calc_coding_agent_cache_hit_rate.py` | Token 统计 |
| 多模态评估 | `image/prompt_image_test.py` | 图片理解测试 |

## 6. 编写轨迹 → ShareGPT 转换脚本（示例）

```python
#!/usr/bin/env python3
"""将 replay.json events 转为 ShareGPT 格式 train.jsonl"""
import json
import sys

def events_to_sharegpt(events):
    conversations = []
    for e in events:
        c = e.get("content", {})
        action = c.get("action", "")
        msg = c.get("message") or c.get("args", {}).get("content", "")

        if action == "system":
            conversations.append({"from": "system", "value": msg})
        elif action in ("message", "user"):
            conversations.append({"from": "human", "value": msg})
        elif action in ("agent", "assistant"):
            turn = {"from": "gpt", "value": msg}
            if c.get("tool_calls"):
                turn["tool_calls"] = c["tool_calls"]
            conversations.append(turn)
        elif action == "tool":
            conversations.append({"from": "tool", "value": msg})

    return {"conversations": conversations}

if __name__ == "__main__":
    data = json.load(open(sys.argv[1]))
    sample = events_to_sharegpt(data["events"])
    print(json.dumps(sample, ensure_ascii=False))
```

可根据实际 event 结构扩展此脚本。
