# 01 数据准备

Coding Agent SFT 的核心是 **多轮工具调用轨迹**。数据质量直接决定微调效果。

## 1. 数据来源

| 来源 | 说明 | 质量 |
|------|------|------|
| **Agent 运行轨迹** | 生产/测试环境真实对话 + 工具调用 | 最高（推荐） |
| **人工标注** | 专家编写 ideal trajectory | 高，成本高 |
| **合成数据** | 强模型生成 + 人工筛选 | 中，可规模化 |
| **开源数据集** | SWE-bench trajectories、OpenHands 等 | 中，需适配 prompt |

### 从 Agent 轨迹提取

典型轨迹结构（OpenHands / 类似框架）：

```
system → user → assistant(tool_calls) → tool(result) → assistant(tool_calls) → ... → assistant(final)
```

sh-mk 工具链：

```
event/get_event.py          → 拉取原始 event
replay/build_replay_template_from_replay_json.py  → 生成 replay 模板
人工筛选 + 格式转换          → train.jsonl
```

## 2. 数据格式

### 2.1 ShareGPT 格式（LLaMA-Factory / ms-swift 通用）

每行一条样本：

```json
{
  "conversations": [
    {"from": "system", "value": "You are a coding assistant..."},
    {"from": "human", "value": "创建一个 React 待办清单应用"},
    {
      "from": "gpt",
      "value": "我来分析需求并创建项目结构。",
      "tool_calls": [
        {
          "name": "str_replace_editor",
          "arguments": "{\"command\": \"create\", \"path\": \"/workspace/app/src/App.tsx\", \"file_text\": \"...\"}"
        }
      ]
    },
    {
      "from": "tool",
      "value": "File created successfully at /workspace/app/src/App.tsx"
    },
    {
      "from": "gpt",
      "value": "应用已创建完成。END_CODE"
    }
  ]
}
```

角色映射：

| from 值 | 对应 OpenAI role |
|---------|-----------------|
| system | system |
| human | user |
| gpt | assistant |
| tool | tool |

### 2.2 OpenAI Messages 格式

```json
{
  "messages": [
    {"role": "system", "content": "You are a coding assistant..."},
    {"role": "user", "content": "创建一个 React 待办清单应用"},
    {
      "role": "assistant",
      "content": "我来分析需求并创建项目结构。",
      "tool_calls": [
        {
          "id": "call_abc123",
          "type": "function",
          "function": {
            "name": "str_replace_editor",
            "arguments": "{\"command\": \"create\", \"path\": \"...\"}"
          }
        }
      ]
    },
    {
      "role": "tool",
      "tool_call_id": "call_abc123",
      "content": "File created successfully"
    },
    {
      "role": "assistant",
      "content": "应用已创建完成。"
    }
  ]
}
```

### 2.3 Alpaca 格式（简单场景，无工具调用）

仅适用于不含 tool use 的简单代码问答：

```json
{
  "instruction": "用 Python 实现快速排序",
  "input": "",
  "output": "def quicksort(arr):\n    ..."
}
```

> Coding Agent 场景**不推荐** Alpaca 格式，会丢失工具调用能力。

## 3. 数据清洗规范

### 3.1 必须保留

- 完整的 system prompt（与推理时一致）
- 所有 tool_calls 及其对应的 tool 回复
- 多轮对话的完整上下文链
- 最终 finish/END_CODE 标记

### 3.2 必须过滤

- 训练失败的轨迹（任务未完成、lint 未通过）
- 包含敏感信息（API Key、密码、内网地址）
- 重复/近似重复样本（去重）
- 异常长的单轮回复（可能是模型幻觉）
- tool_call 参数含 placeholder 的样本

### 3.3 质量筛选建议

```python
# 伪代码：筛选高质量轨迹
def is_high_quality(trajectory):
    return (
        trajectory.task_completed          # 任务成功完成
        and trajectory.lint_passed         # lint 通过
        and len(trajectory.tool_calls) >= 3  # 有实际工具调用
        and trajectory.total_tokens < 32000  # 未超上下文
        and not trajectory.has_errors      # 无严重错误
    )
```

### 3.4 数据量建议

| 场景 | 最低量 | 推荐量 |
|------|--------|--------|
| LoRA 微调 | 500 条 | 2K–5K 条 |
| Full SFT | 2K 条 | 5K–20K 条 |
| 特定领域（如前端） | 200 条 | 1K–3K 条 |

## 4. 格式转换

### ShareGPT ← OpenAI Messages

```python
ROLE_MAP = {"system": "system", "user": "human", "assistant": "gpt", "tool": "tool"}

def to_sharegpt(messages):
    conversations = []
    for msg in messages:
        turn = {"from": ROLE_MAP[msg["role"]], "value": msg.get("content", "")}
        if msg.get("tool_calls"):
            turn["tool_calls"] = [
                {"name": tc["function"]["name"], "arguments": tc["function"]["arguments"]}
                for tc in msg["tool_calls"]
            ]
        conversations.append(turn)
    return {"conversations": conversations}
```

### 从 replay.json 提取

sh-mk 的 replay 文件包含完整 event 链，可按 event 类型过滤：

```
system  event  → conversations[0]
user    event  → conversations[n] from=human
agent   event  → conversations[n] from=gpt (+ tool_calls)
tool    event  → conversations[n] from=tool
```

## 5. 数据集划分

```
total dataset
├── train.jsonl    90%  训练
├── val.jsonl       5%  训练过程验证 loss
└── test.jsonl      5%  最终 benchmark（不参与训练）
```

- 按 **任务类型** 分层采样（Web/H5/小程序等），避免某类任务过拟合
- 同一 app_id 的轨迹不要同时出现在 train 和 test

## 6. 注册到训练框架

### LLaMA-Factory

在 `data/dataset_info.json` 中添加：

```json
{
  "coding_agent_sft": {
    "file_name": "coding_agent_train.jsonl",
    "formatting": "sharegpt",
    "columns": {
      "messages": "conversations"
    },
    "tags": {
      "role_tag": "from",
      "content_tag": "value",
      "user_tag": "human",
      "assistant_tag": "gpt",
      "system_tag": "system"
    }
  }
}
```

### ms-swift

```bash
# 直接使用 jsonl，指定 columns
swift sft \
  --dataset ./coding_agent_train.jsonl \
  --columns '{"prompt": "query", "response": "response"}' \
  ...
```

对于 tool calling 数据，推荐使用 ShareGPT 格式 + 对应 template。

## 7. 检查清单

- [ ] 每条样本 JSON 可正常 parse
- [ ] system prompt 与推理配置一致
- [ ] tool_calls 与 tool 回复一一对应
- [ ] 无敏感信息泄露
- [ ] train/val/test 无数据泄漏（同 app_id 不跨集）
- [ ] 样本量满足最低要求
- [ ] 已注册到训练框架的 dataset_info
